
extern fn printf(str: i8*, ...): void;

struct Point {
    x: i32;
    y: i32;
}

fn main(): i32 
{
    let a: Point = Point {
        .x = 5,
        .y = 11
    };

    printf("Point (%d, %d)\n", a.x, a.y);

    return 1;
}