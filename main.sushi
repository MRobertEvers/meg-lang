
struct Point {
    x: i8;
    y: i8;
}

fn func(a: i32): i32 {
    let p: Point;
    return a;
}

fn main(): i32 {
    let a: i8 = 5;
    let b: i32 = a;

    if (a > 8) {
        return 11;
    } else if (b > a) {
        return 8;
    } else {
        return 0;
    }

    return func(b) * 4;
}