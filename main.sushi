extern fn printf(str: i8*, ...): void;


enum Node {
    Void,
    String { ptr: i8*; size: i32 }
    Point {
        x: i32;
        y: i32;
    }
}



fn foo(a: i32): i32 {
    return a;
}

fn main(): i32 
{
    let np: Node = Node::Point {
        .x = 9,
        .y = 11
    };



    let big: i32 = 88;

    let small: i8 = 8;

    small = big;

    if (big > 8)
    {
        return foo(big);
    }

    return 1;
}