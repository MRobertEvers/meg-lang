struct my_nested {
    n: i32;
}

struct my_struct {
    a: i32;
    b: my_nested;
}

fn callable(a: i32, b: i32): i32 {
    return a + b;
}

fn func(s:
 my_struct*): i32 // This is my comment
{
    let a = s.a;

    // A comment
    // Another comment
    if (a > 5) {
        a = 5;
    }

    while (80 > a) {
        a = a + 1;
    }

    return (callable(s.b.n, a) * 5);
}
