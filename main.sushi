extern fn printf(str: i8*, ...): void;

fn iter_foo(): impl i32 {
    for (let i = 0; i < 5; i += 1)
    {
        yield i;
    }

    return;
}

fn main(): i32 
{
    let iter = iter_foo();

    return 1;
}