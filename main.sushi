

extern fn printf(str: i8*, ...): void;

enum Literal {
    Void,
    String { size: i32; bytes: i8* }
    Number { value: i32 }
}

fn main(): i32 
{
    let a: Literal;

    if (a is Literal::Number) => (a_num: Literal::Number) {
        printf("%d\n", a_num.value);
    }

    return 1;
}