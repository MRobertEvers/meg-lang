extern fn printf(c: i8*, ...): void;

async fn my_coro(a: i32): impl Generator<i32, i32, i32> {
    let first = yield a;

    return first;
}

fn main(): i32 {
    let frame = my_coro(3);

    let arg = frame.begin();

    let yielded = frame.send(1);

    return 0;
}