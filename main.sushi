

extern fn printf(str: i8*, ...): void;

struct Point {
    a: i32;
    b: i32;
    c: i32;
    x: i32;
    y: i32;
}

fn main(): i32 
{
    for (let i = 0; i < 5; i = i + 1)
    {
        printf("%d, ", i);
    }

    return 1;
}