


struct Point {
    x: i8;
    y: i8;
}

async fn range(a: i32, b: i32): impl generator<i32, void, void> {
    for (let i = a; i < b; i = i + 1)
        yield i;
}


enum AstNode {
    None,
    Func { id: i32 }
}

fn P(p: Point): Point {
    return p;
}

fn func(a: i32): i32 {
    let p: Point;
    P(p);
    return a;
}

fn main(): i32 {
    return func(2) * 4;
}