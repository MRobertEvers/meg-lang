

extern fn printf(str: i8*, ...): void;

struct Point {
    a: i32;
    b: i32;
    c: i32;
    x: i32;
    y: i32;
}

fn main(): i32 
{
    let i = 0;
    while (i < 5)
    {
        printf("%d", i);
        i = i + 1;
    }

    return 1;
}