

struct Wind {
    i: i32;
}

struct Point {
    x: i8;
    y: i8;
    w: Wind;
}

async fn range(a: i32, b: i32): impl generator<i32, void, void> {
    for (let i = a; i < b; i = i + 1)
        yield i;
}


enum AstNode {
    None,
    Func { id: i32 }
}

fn main(): i32 {
    let arr: i8*;

    let elem = arr[4];

    return 4;
}