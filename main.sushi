extern fn printf(c: i8*, ...): void;

struct Point {
    x: i32;
    y: i32;
}

fn x(a: Point*): i32 {
    return a->x;
}

fn main(): i32 {
    let p: Point = Point {
        .x = 5,
        .y = 6
    };

    let l: i64 = x(&p);
    printf("%d\n", l);

    return 1;
}