

extern fn printf(str: i8*): void;

struct Point {
    x: i32;
    y: i32;
}

fn main(): i32 
{
    let billy: Point = Point;
    billy.x = 8;
    billy.y = 9;

    let harg = billy.y;
    let my_var = 5*6 + harg;
    printf("Hello world");
    return my_var;
}