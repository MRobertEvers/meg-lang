extern fn printf(str: i8*, ...): void;


fn foo(a: i32): i32 {
    return a;
}

fn main(): i32 
{
    let big: i32 = 88;

    let small: i8 = 8;

    small = big;

    if (big > 8)
    {
        return foo(big);
    }

    return 1;
}