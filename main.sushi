
struct willy {
    a: i16;
    b: i32;
}

fn callable(s: willy*): i32 {

    let a = 1;

    a *= 7;
    if (a >= 0  ) {
        a = 0;
    } 

    for (let l = 5; l > 0; l = l - 1) {
        a = a + l;
    }

    return a;
}
